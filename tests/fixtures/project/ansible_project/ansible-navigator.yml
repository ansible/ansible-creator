---
ansible-navigator:
  logging:
    level: debug
    append: False
    file: $PWD/.logs/ansible-navigator.log

  playbook-artifact:
    enable: True
    save-as: "$PWD/.logs/{playbook_name}-artifact-{time_stamp}.json"
