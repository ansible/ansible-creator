---
argument_specs:
  argspec_validation: # The actual name of the play
    short_description: Data validation for <play_name> # Example play name is argspec_validation
    description:
      - argspec_validation is the name of the play within the example playbook.
      - Here we can describe what the play does.
    author:
      - Automation Team
    options:
      var_one:
        description:
          - "var_one is a required variable that must be provided."
          - "It can be used to specify the first parameter of the play."
        default: "default_value"
        required: true
        type: str
      var_two:
        description:
          - "This is a description for var_two, another required variable"
          - "It can be used to specify the second parameter of the play."
        required: true
        type: int
      var_three:
        description:
          - "var_three the last required variable that must be provided."
          - "It can be used to specify the third parameter of the play."
        required: true
        type: dict
